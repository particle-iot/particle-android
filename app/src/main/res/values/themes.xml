<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="AppTheme" parent="@style/Theme.MaterialComponents.Light.Bridge">
        <item name="colorPrimary">@color/primary_color</item>

        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/primary_color_dark</item>

        <!-- colorAccent is used as the default value for colorControlActivated,
             which is used to tint widgets -->
        <item name="colorAccent">@color/accent_color</item>

        <!-- Opt out of the Android 15 (targetSdk 35) edge-to-edge enforcement so the system
             insets content below the status bar / display cutout, instead of drawing the
             toolbar underneath it. NOTE: this opt-out is ignored from targetSdk 36 (Android 16)
             onward; the proper fix is to consume WindowInsets per-screen. Ignored on API < 35. -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>

<!--        <item name="colorButtonNormal">@color/primary_color</item>-->
<!--        <item name="colorControlNormal">@color/primary_color</item>-->
        <!-- You can also set colorControlNormal, colorControlActivated
             colorControlHighlight, and colorSwitchThumbNormal. -->
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar">
        <item name="colorButtonNormal">@color/primary_color</item>
        <item name="colorControlNormal">@color/primary_color</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />


</resources>