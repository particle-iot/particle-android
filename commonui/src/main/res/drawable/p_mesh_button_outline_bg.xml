<?xml version="1.0" encoding="utf-8"?>
<!-- Outlined cyan background for p_mesh_button_style_outline. Set explicitly so the button
     renders visibly (the MaterialComponents OutlinedButton style produces no visible stroke
     when applied to a plain AppCompatButton). -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke android:width="2dp" android:color="#9E9E9E" />
            <corners android:radius="4dp" />
        </shape>
    </item>
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#1A00AEEF" />
            <stroke android:width="2dp" android:color="@color/p_particle_cyan" />
            <corners android:radius="4dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke android:width="2dp" android:color="@color/p_particle_cyan" />
            <corners android:radius="4dp" />
        </shape>
    </item>
</selector>
